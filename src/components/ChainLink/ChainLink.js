import React, { Component } from 'react';
import cn from "classnames";

class CaptionedImage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (

        );
    }
}

export default CaptionedImage;
